<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcmseek.dao.UserWebMapper">

    <resultMap type="com.tcmseek.pojo.UserWeb" id="UserWebResult">
        <id     property="id"           column="id"             />
        <result property="username"     column="username"       />
        <result property="account"      column="account"        />
        <result property="password"     column="password"       />
        <result property="email"        column="email"          />
        <result property="createTime"   column="create_time"    />
        <result property="updateTime"   column="update_time"    />
    </resultMap>

    <sql id="selectUserWebVo">
        select id, username, account, password, email, create_time, update_time
        from user_web
    </sql>
    <insert id="insert">
        insert into user_web(username, account, password, email, create_time, update_time)
        values(#{username}, #{account}, #{password}, #{email}, #{createTime}, #{updateTime})
    </insert>

    <select id="selectByAccount" parameterType="String" resultMap="UserWebResult">
        <include refid="selectUserWebVo"/>
        where account = #{account}
    </select>

    <select id="selectByAccountAndPassword" resultMap="UserWebResult">
        <include refid="selectUserWebVo"/>
        where account = #{account} and password = #{password}
    </select>

</mapper>



